# Supabase (optional — enables auth, cloud palettes, profiles, and match history)
# Create a project at https://supabase.com, then paste your project URL and anon key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — required for uploading generated voice clips to Supabase Storage (Vercel production)
SUPABASE_SERVICE_ROLE_KEY=

# Setup:
# 1. Copy this file to .env.local and fill in the values above.
# 2. Run migrations in order in the Supabase SQL editor
#    (or use the Supabase CLI: supabase link && supabase db push):
#    - supabase/migrations/20260705183000_initial_schema.sql
#    - supabase/migrations/20260705210000_player_stats_and_avatars.sql
#    - supabase/migrations/20260708190000_voice_clips_storage.sql
# 3. In Supabase Dashboard → Authentication → URL Configuration, add:
#    - Site URL: http://localhost:3000 (or your production origin)
#    - Redirect URLs: http://localhost:3000/auth/callback
#      (and https://your-domain/auth/callback for production)
# 4. Email/password is enabled by default. Keep "Confirm email" enabled under
#    Authentication → Providers → Email.
# 5. Confirm signup email: edit supabase/templates/confirm-signup.html (styled OTP)
#    or run: npm run configure:resend-smtp (pushes template + Resend SMTP to Supabase).
#    OTP length is 6 in Supabase (must match features/auth/lib/email-otp.ts).

# Resend (Supabase Auth SMTP — verification emails)
# API key: https://resend.com/api-keys
# Supabase access token: https://supabase.com/dashboard/account/tokens
RESEND_API_KEY=
SUPABASE_ACCESS_TOKEN=
RESEND_SENDER_EMAIL=jgelsomino@foundry360.us
RESEND_SENDER_NAME=VectorOS

# Voice announcements (Sadachbia and other Gemini voices)
# Required: create a key at https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY=
# Gemini TTS model (default: gemini-2.5-pro-preview-tts)
GOOGLE_GEMINI_TTS_MODEL=
# Voice name (default: Sadachbia)
GOOGLE_CLOUD_TTS_VOICE=
# Delivery style — quote multi-word values so dotenv does not truncate them
GOOGLE_CLOUD_TTS_PROMPT=

# Optional — only needed for classic Neural/Wavenet voices (not Sadachbia)
GOOGLE_CLOUD_TTS_API_KEY=
GOOGLE_CLOUD_TTS_SPEAKING_RATE=
GOOGLE_CLOUD_TTS_PITCH=

# Must match voice worker — see docs/VOICE.md and docs/VOICE-VPS.md
NEXT_PUBLIC_VOICE_CLIP_PROFILE=kokoro-bm-george
VOICE_CLIP_PROFILE=
# Production: Kokoro on Linux VPS (nginx + stable subdomain).
# Full guide: docs/VOICE.md  |  VPS setup: docs/VOICE-VPS.md  |  Worker: services/voice-worker/README.md
# macOS dev (npm run dev): built-in `say` + Daniel — no worker needed.
# Vercel production requires:
#   VOICE_SYNTHESIS_URL  — stable HTTPS worker URL (e.g. https://voice.foundry360.us)
#   VOICE_SYNTHESIS_TOKEN — must match services/voice-worker/.env on the VPS
#   SUPABASE_SERVICE_ROLE_KEY — uploads generated clips to the voice-clips bucket
VOICE_SYNTHESIS_URL=
VOICE_SYNTHESIS_TOKEN=
# Legacy — only if running Piper directly on Linux (not used with Kokoro Docker stack)
PIPER_BIN=piper
PIPER_MODEL_PATH=
LOCAL_SAY_TURN_VOICE=Daniel (English (UK))
LOCAL_SAY_TURN_RATE=168
LOCAL_SAY_CACHE_DIR=

# Stripe (wallet / payments — server routes and webhooks write billing rows)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Recurring price IDs from Stripe Dashboard → Products
STRIPE_PRICE_CLUB=price_...
STRIPE_PRICE_ELITE=price_...
# Optional: create matching promotion codes in Stripe (e.g. LAUNCH20, DARTS10, OS100)
